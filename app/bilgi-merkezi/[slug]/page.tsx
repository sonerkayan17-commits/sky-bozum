import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { articles, getArticle } from "../../lib/site";
import { ArrowIcon, CheckIcon } from "../../components/ui/Icons";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=getArticle(slug);return a?{title:a.title,description:a.excerpt}:{} }
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=getArticle(slug);if(!a)notFound();return <main className="bg-white"><section className="bg-slate-950 text-white"><div className="mx-auto max-w-4xl px-5 py-20 lg:px-8 lg:py-24"><div className="flex gap-3 text-xs font-black uppercase tracking-[.16em] text-blue-300"><Link href="/bilgi-merkezi">Bilgi Merkezi</Link><span>/</span><span>{a.category}</span></div><h1 className="mt-7 text-4xl font-black leading-tight tracking-[-.045em] sm:text-6xl">{a.title}</h1><p className="mt-6 text-lg leading-8 text-slate-300">{a.excerpt}</p><p className="mt-5 text-sm font-bold text-slate-400">Okuma süresi: {a.readTime}</p></div></section><article className="mx-auto max-w-4xl px-5 py-16 lg:px-8 lg:py-24">{a.sections.map((s,i)=><section key={s.title} className={i?'mt-16':''}><h2 className="text-3xl font-black tracking-[-.035em] text-slate-950 sm:text-4xl">{s.title}</h2><div className="mt-6 space-y-5 text-base leading-8 text-slate-700">{s.paragraphs.map(p=><p key={p}>{p}</p>)}</div>{s.bullets&&<div className="mt-6 space-y-3">{s.bullets.map(b=><div key={b} className="flex gap-3"><CheckIcon className="mt-1 h-5 w-5 shrink-0 text-blue-700"/><p className="leading-7 text-slate-700">{b}</p></div>)}</div>}</section>)}{a.serviceSlug&&<div className="mt-16 rounded-[30px] bg-slate-950 p-8 text-white"><h2 className="text-3xl font-black">İşleme hazır mısınız?</h2><p className="mt-4 text-slate-300">Güncel oran ve uygunluk bilgisini işlem öncesinde alın.</p><Link href={`/hizmetler/${a.serviceSlug}`} className="mt-6 inline-flex min-h-13 items-center gap-3 rounded-full bg-white px-6 text-sm font-black text-slate-950">Hizmeti incele <ArrowIcon/></Link></div>}</article></main>}
