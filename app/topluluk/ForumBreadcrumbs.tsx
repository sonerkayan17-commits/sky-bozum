import Link from'next/link';import{forumRoutes}from'../lib/forumRoutes';import'./forum-breadcrumbs.css';
type Props={section?:{slug:string;title:string};category?:{slug:string;title:string};topic?:string};
export default function ForumBreadcrumbs({section,category,topic}:Props){return <nav className="forum-breadcrumbs" aria-label="Forum konum yolu"><Link href={forumRoutes.home}>Forum</Link>{section&&<><span aria-hidden="true">›</span>{category||topic?<Link href={forumRoutes.section(section.slug)}>{section.title}</Link>:<b aria-current="page">{section.title}</b>}</>}{section&&category&&<><span aria-hidden="true">›</span>{topic?<Link href={forumRoutes.category(section.slug,category.slug)}>{category.title}</Link>:<b aria-current="page">{category.title}</b>}</>}{topic&&<><span aria-hidden="true">›</span><b aria-current="page">{topic}</b></>}</nav>}
