import Link from "next/link";import ServiceCard from "./services/ServiceCard";import {services} from "../lib/site";export default function Services(){return <section className="bg-[#f6f8fc] px-5 py-20 lg:px-8 lg:py-28"><div className="mx-auto max-w-7xl"><div className="max-w-3xl"><p className="text-sm font-black uppercase tracking-[.2em] text-blue-700">Öne çıkan hizmetler</p><h2 className="mt-4 text-4xl font-black tracking-[-.04em] text-slate-950 sm:text-6xl">En çok tercih edilen işlemler</h2><p className="mt-5 text-lg leading-8 text-slate-600">Güncel oran, açıklama ve işlem adımlarına tek tıkla ulaşın.</p></div><div className="mt-12 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{services.filter(s=>s.popular).slice(0,6).map(s=><ServiceCard key={s.slug} service={s}/>)}</div><div className="mt-10 text-center"><Link href="/hizmetler" className="inline-flex min-h-14 items-center rounded-full bg-slate-950 px-7 text-sm font-black text-white">Tüm hizmetleri görüntüle</Link></div></div></section>}
