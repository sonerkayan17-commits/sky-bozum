'use client';
import {useEffect,useRef,useState} from'react';
type Props={value:string;onChange:(value:string)=>void;placeholder?:string;rows?:number};
const allowedTags=new Set(['P','DIV','BR','SPAN','FONT','STRONG','B','EM','I','U','S','H2','H3','UL','OL','LI','BLOCKQUOTE','A','IMG']);
const safeFonts=new Set(['Arial','Georgia','Tahoma','Trebuchet MS','Verdana']);
export function sanitizeArticleHtml(source:string){if(typeof window==='undefined')return source;const safeDocument=document.implementation.createHTMLDocument('article');safeDocument.body.innerHTML=source;[...safeDocument.body.querySelectorAll('*')].forEach(element=>{if(!allowedTags.has(element.tagName)){element.replaceWith(...Array.from(element.childNodes));return}[...element.attributes].forEach(attribute=>{const name=attribute.name.toLowerCase(),value=attribute.value;if(element.tagName==='A'&&name==='href'&&/^(https?:|mailto:|\/)/i.test(value))return;if(element.tagName==='IMG'&&name==='src'&&/^(https?:|\/)/i.test(value))return;if(element.tagName==='IMG'&&name==='alt')return;if(name==='align'&&/^(left|center|right|justify)$/i.test(value))return;if(element.tagName==='FONT'&&name==='face'&&safeFonts.has(value))return;if(element.tagName==='FONT'&&name==='size'&&/^[2-5]$/.test(value))return;element.removeAttribute(attribute.name)});if(element.tagName==='A'){element.setAttribute('rel','nofollow noopener noreferrer');element.setAttribute('target','_blank')}if(element.tagName==='IMG')element.setAttribute('loading','lazy')});return safeDocument.body.innerHTML.trim()}
export default function RichArticleEditor({value,onChange,placeholder}:Props){const editorRef=useRef<HTMLDivElement>(null);const[preview,setPreview]=useState(false);useEffect(()=>{if(editorRef.current&&editorRef.current.innerHTML!==value)editorRef.current.innerHTML=value},[value,preview]);const commit=()=>onChange(sanitizeArticleHtml(editorRef.current?.innerHTML||''));const command=(name:string,value?:string)=>{editorRef.current?.focus();document.execCommand(name,false,value);commit()};const addLink=()=>{const href=window.prompt('Bağlantı adresi');if(href&&/^(https?:|mailto:|\/)/i.test(href))command('createLink',href)};const addImage=()=>{const src=window.prompt('Görsel adresi veya site içi yol (örn. /images/kapak.webp)');if(!src||!/^(https?:|\/)/i.test(src))return;const alt=window.prompt('Görsel açıklaması (alt metin)')||'';editorRef.current?.focus();document.execCommand('insertHTML',false,`<img src="${src.replace(/"/g,'&quot;')}" alt="${alt.replace(/"/g,'&quot;')}">`);commit()};return <div className="rich-editor"><div className="rich-editor-toolbar" role="toolbar" aria-label="Metin biçimlendirme araçları"><button type="button" title="Kalın" onClick={()=>command('bold')}><b>B</b></button><button type="button" title="İtalik" onClick={()=>command('italic')}><i>I</i></button><button type="button" title="Altı çizili" onClick={()=>command('underline')}><u>U</u></button><button type="button" title="Üzeri çizili" onClick={()=>command('strikeThrough')}><s>S</s></button><span/><button type="button" title="Başlık" onClick={()=>command('formatBlock','h2')}>H2</button><button type="button" title="Alt başlık" onClick={()=>command('formatBlock','h3')}>H3</button><button type="button" title="Alıntı" onClick={()=>command('formatBlock','blockquote')}>❝</button><span/><select title="Yazı tipi" defaultValue="Arial" onChange={e=>command('fontName',e.target.value)}><option>Arial</option><option>Georgia</option><option>Tahoma</option><option>Trebuchet MS</option><option>Verdana</option></select><select title="Yazı boyutu" defaultValue="3" onChange={e=>command('fontSize',e.target.value)}><option value="2">Küçük</option><option value="3">Normal</option><option value="4">Büyük</option><option value="5">Çok büyük</option></select><span/><button type="button" title="Sola hizala" onClick={()=>command('justifyLeft')}>≡</button><button type="button" title="Ortala" onClick={()=>command('justifyCenter')}>☰</button><button type="button" title="Sağa hizala" onClick={()=>command('justifyRight')}>≣</button><button type="button" title="İki yana yasla" onClick={()=>command('justifyFull')}>▤</button><span/><button type="button" title="Madde listesi" onClick={()=>command('insertUnorderedList')}>•≡</button><button type="button" title="Numaralı liste" onClick={()=>command('insertOrderedList')}>1≡</button><button type="button" title="Bağlantı ekle" onClick={addLink}>🔗</button><button type="button" title="Görsel ekle" onClick={addImage}>▧</button><button type="button" title="Biçimi temizle" onClick={()=>command('removeFormat')}>Tx</button><span/><button type="button" className={preview?'is-active':''} title="Önizleme" onClick={()=>setPreview(current=>!current)}>◉ Önizleme</button></div>{preview?<div className="rich-editor-preview" dangerouslySetInnerHTML={{__html:sanitizeArticleHtml(value)}}/>:<div ref={editorRef} className="rich-editor-canvas" contentEditable suppressContentEditableWarning role="textbox" aria-multiline="true" aria-label="Konu metni" data-placeholder={placeholder||'Konunuzu yazın. Biçimlendirme araçlarını üst menüden kullanabilirsiniz.'} onInput={commit} onBlur={commit}/>}<p className="rich-editor-note">Başlık, yazı tipi, hizalama, liste, bağlantı ve görseller desteklenir. Yayınlamadan önce önizleyebilirsiniz.</p></div>}
